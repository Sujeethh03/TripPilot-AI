# Progress

Running log of what's done, in flight, and next. Update this as we work. Newest entries at the top of the log.

## Status

**Phase:** MVP feature-complete (backend + frontend) → integration polish
**Current focus:** End-to-end verified; next is real-data grounding (directions MCP, Researcher wiring) + deploy

## Now / Next

- [ ] Live browser click-through of the full UI (register → new trip → chat → itinerary → PDF); the route map needs a real `NEXT_PUBLIC_GOOGLE_MAPS_KEY` (Maps JS + Geocoding APIs enabled) to render — hidden until then
- [ ] Deploy (backend → Replit, frontend → Vercel)
- [ ] Validator drive-time rule — needs per-block coords in the Itinerary schema first (directions MCP is ready)
- [ ] Intake: date/duration resolution + `interrupt()` for missing required fields
- [ ] buses MCP server (Rome2Rio — deferred; API access is gated/paid)

## Done

- [x] Starting point (own transport) + hotels ("where to stay"). **Origin/transport**: new optional `origin` + `transport_mode` (drive/transit) on the trip — added to the `Trip` model (+ Alembic migration `a1b2c3d4e5f6`), `TripCreate`/`TripResponse`, `TripRequest`/`IntakeExtraction` (intake prompt now extracts them), and the `_trip_context` seed so the agents know the starting point. The Researcher computes the **travel leg** via the existing directions MCP (`_travel_leg`, honouring the chosen mode); the Synthesizer attaches it **deterministically** (`_attach_grounding`, a fact — not the LLM), alongside origin. Surfaced as a travel banner in the itinerary UI + a "Getting there" line in the PDF. **Hotels**: new `search_hotels` tool on the existing **places** MCP server (Google Places Text Search with `includedType=lodging`; `_client.search_hotels` + `app/mcp/places.search_hotels` helper — one server, two tools, KISS). Researcher fetches 4 lodging options for the destination; Synthesizer attaches them deterministically; shown as a "Where to stay" section in the UI + PDF. **Honest limits**: hotels are names/area/rating only — no live prices/availability (needs a paid API). New SSOT fields on `Itinerary`: `origin`, `travel: TravelLeg`, `hotels: list[HotelOption]`; Refiner carries these facts across revisions (`_carry_facts`) so a refine turn can't drop/invent them. Frontend: Zod mirrors, form gets "Starting from" + transport selector, `TravelBanner` + `HotelList` components, `formatDuration`. The route map stays scoped to daily stops (earlier explicit choice) — origin lives in the banner to avoid zooming the map out to the origin city. **LIVE verified**: a Kochi→Munnar drive trip produced a real travel leg (126.3 km / ~222 min) + 4 real Munnar hotels (★4.3–4.8), and the PDF rendered both sections. New tests: `test_search_hotels_uses_lodging_type`, `test_synthesizer_attaches_travel_and_hotels`. 98 backend tests green, mypy (app + mcp_servers) strict + ruff clean; frontend tsc + lint + production build green.
- [x] Fixed "AI messages don't show in chat — only my own text". Two real bugs. **(1) Planning turns emitted no chat text at all**: the conversation_manager only produces an `assistant_message` for chit_chat/clarify; for `plan`/`refine` the "reply" was just the itinerary, and `_persist_turn` even wrote an empty (`''`) assistant row. Now `_run_turn` synthesizes a short acknowledgment (`_plan_summary`, deterministic from the itinerary's days + total cost — no LLM, no invented facts) whenever a turn produced an itinerary but no text, streams it as an `assistant_message`, and persists it; `_persist_turn` no longer writes empty assistant rows. **(2) The trip view reseeded the chat store from stale server history on every trip refetch**: the seed effect keyed on `[messages, trip]`, so after a plan turn updated the itinerary (→ trip cache invalidated → new `trip` ref) it re-ran `seed(...)` and clobbered the messages streamed that session. Now seeds **once per trip** (a `seeded` ref, reset on trip change/unmount alongside the store `reset` + kickoff guard), and the socket invalidates the `messages` cache too on complete so a later remount reseeds correctly. **LIVE verified**: a Goa plan turn now streams *and* persists `"I've put together a 3-day Goa itinerary for around ₹7,500. It's on the right — tell me what you'd like to change."` New tests: `test_persist_turn_skips_empty_assistant`, `test_plan_summary_uses_itinerary_facts`. 96 backend tests green, ruff/mypy strict clean; frontend tsc + lint clean.
- [x] Weather + route map surfaced in the itinerary UI. **Weather**: the Researcher already fetched a forecast (`ResearchBundle.weather_by_day`) but it was discarded — never in the SSOT. Added an optional `Weather` (min/max °C + condition) to each `Day` in the canonical `Itinerary` schema and attach it **deterministically post-synthesis** (`synthesizer._attach_weather` zips the forecast, insertion-ordered by day, onto the itinerary days; days beyond the 5-day forecast window stay `None`, and any LLM-set weather is overwritten — "never trust the LLM for facts"). Synthesizer prompt told to leave `weather` null. Flows through streaming, DB persistence, and PDF (`_weather_line`, "27/13C light rain"). Frontend: Zod `weatherSchema` mirror, a per-day weather badge (emoji via `weatherEmoji` + temp range) in `ItineraryTimeline`. **LIVE verified**: a Munnar plan attached the real forecast to all 3 days (13.4/19.9°C "light rain"); PDF renders with weather (valid `%PDF`). **Route map**: new `RouteMap` (`@vis.gl/react-google-maps`, key `NEXT_PUBLIC_GOOGLE_MAPS_KEY`) above the timeline — plots the itinerary's block locations as numbered markers in visiting order joined by a route line; locations are geocoded client-side (blocks carry only a name). Renders nothing if no key is set or there are no stops. Added `@vis.gl/react-google-maps` + `@types/google.maps`; env var documented in both `.env.local(.example)`. New test `test_synthesizer_attaches_weather_from_research`. 94 backend tests green, ruff/mypy strict clean; frontend tsc/lint/production-build green.
- [x] Delete-trip feature (full purge). `DELETE /trips/{id}` changed from soft-delete to a **hard delete of all trip data**: the trip row + its conversation + messages (via ON DELETE CASCADE) and the LangGraph checkpoint for the thread (`checkpointer.adelete_thread(trip_id)`, best-effort, works for both Memory + Postgres savers). `TripRepository.hard_delete`. Frontend: `TripCard` now has a three-dots (`⋮`) actions menu (shadcn `dropdown-menu`) rendered as a sibling of the card Link so it never navigates; Delete opens a confirmation `alert-dialog` → `useDeleteTrip` mutation → list invalidates and the card vanishes. Tests: `test_delete_purges_all_trip_data` (asserts trip/conversation/message rows all gone). LIVE verified against the running server: a planned Ooty trip (itinerary + 2 messages) → delete 204 → trip 404, messages 404, list empty, no checkpoint errors. 93 backend tests green, ruff/mypy clean; frontend tsc/lint/build green.
- [x] Trip form details now seed the conversation (fixes "AI re-asks for details already in the form"). The chat WS handler builds a one-line context string from the trip's destination/dates/budget (`_trip_context`) and, on the **first turn of a fresh thread**, injects it as a system message so Intake merges it with the user's text instead of re-asking; dates are converted to duration_days. Frontend: "Create & start planning" now navigates with `?start=1` and the trip view auto-sends a kickoff turn once (when connected + no history), so planning begins straight from the form. LIVE verified: a Goa trip (₹18k, Aug 10–13) with only "Let's plan my trip." routed to `plan` and produced a correct 4-day Goa itinerary — no clarifying questions. Backend ruff/mypy + frontend tsc/lint clean; chat/graph tests green.
- [x] Researcher grounded with real data + `directions` MCP server. Researcher now fetches weather **and** real candidate places in parallel (`app/mcp/places.search_places`): one text search per day theme/target-area (`_place_queries`, capped at 5 × 4 results, de-duped by place_id/name), populating `ResearchBundle.candidate_places` — which the Synthesizer already consumes ("prefer researched places"). **LIVE verified**: a Munnar plan now cites real Google Places with addresses (Pallivasal Tea Estate, Attukad Waterfalls, Nayamakad Waterfall) instead of invented spots. New `directions` MCP server (FastMCP, `get_directions(origin, destination, mode)` over the Google Directions API, same `GOOGLE_MAPS_KEY`, defensive parsing, `DirectionsResult`) + `app/mcp/directions.py` helper + registry entry. Tests: 4 adapter + 1 tool contract; pool test asserts all 4 tools. LIVE verified via direct client AND full pool path (Kochi→Munnar 126km/222min, Kochi→Alleppey 54km/93min). Drive-time Validator rule deferred (needs per-block coords in the Itinerary schema). 92 tests green, ruff + mypy strict clean.
- [x] Frontend feature-complete (FRONTEND_PLAN steps 3–7): **Dashboard** (`useTrips` list, `TripCard`, `NewTripDialog` with RHF+Zod → `useCreateTrip` → navigate, skeletons + empty state). **Trip view** (`app/trip/[id]`) split chat/itinerary layout with a mobile Chat/Itinerary tab toggle + connection indicator. **Chat** (`MessageList` auto-scroll seeded from `/messages`, `Composer` Enter-to-send). **Streaming** (`lib/ws.ts` + `useChatSocket` — token via query param, backoff reconnect, trip-cache invalidation on complete — feeding a Zustand `store/chat.ts`; `AgentActivity` shows route badge / agent lines / validation / errors). **Itinerary** (`ItineraryTimeline` prefers the live streaming itinerary, falls back to persisted; `BudgetSummary` derived with over/within-budget). **PDF** download (blob, disabled until persisted). Added `zustand`; shadcn `dialog`/`textarea`/`badge`/`scroll-area`/`separator`/`skeleton`. tsc + lint + production build all green. **Streaming live-verified end-to-end**: WS plan turn showed route→intake→planner→researcher→synthesizer(1 day)→validation(invalid)→refiner(3 days)→validation(valid)→complete (₹7500), and `trip.itinerary` + 2 messages persisted. Also wrote root README run steps + `frontend/.env.local`; verified register→login→/me live.
- [x] Frontend auth (FRONTEND_PLAN step 2): login + register pages (React Hook Form + Zod via `lib/forms.ts`), `GoogleSignInButton` (loads Google Identity Services, exchanges the ID token at `/auth/google`; renders only when `NEXT_PUBLIC_GOOGLE_CLIENT_ID` is set) + ambient `types/google.d.ts`, `AuthGuard` client component (validates the token via `/me`, redirects to `/login`, shows a spinner while checking), token persistence across refresh, logout (clears token + TanStack cache). Landing page CTAs → register/login; minimal guarded `/dashboard` placeholder closes the auth loop (fleshed out in step 3). shadcn `input`/`label`/`card` added via `shadcn@2` (classic Radix). Added `react-hook-form` + `@hookform/resolvers`. Routes `/ /login /register /dashboard` all build; tsc + lint + build green. Live browser click-through deferred until dashboard/chat exist.
- [x] Frontend contract layer (FRONTEND_PLAN step 1): `lib/schemas.ts` (Zod mirrors of the backend — Itinerary/Block/Day, ValidationIssue, auth+trip+message DTOs, and the WS event discriminated union on `type`), `lib/auth.ts` (in-memory + localStorage token storage), `lib/api.ts` (typed fetch client: `authApi` + `tripsApi`, bearer-token injection, `ApiError`, Zod-validated responses, PDF-as-Blob, 401 clears token), `lib/query.ts` (centralized query keys + `useTrips`/`useTrip`/`useMessages`/`useCurrentUser` queries + create/update/delete mutations with cache invalidation), `app/providers.tsx` (QueryClientProvider with 401-aware retry) wired into the root layout. Added `zod` + `@tanstack/react-query`. Pure contract layer, no UI yet. tsc --noEmit + lint + build all green.
- [x] Frontend scaffold (FRONTEND_PLAN step 0): Next.js 14 (App Router, TS strict, Tailwind 3) under `frontend/`, pnpm via corepack. shadcn set up in **classic Radix + Tailwind-v3 style** — the latest shadcn CLI defaults to base-ui + Tailwind v4 ("base-nova"), which is bleeding-edge; reset to the standard Radix/HSL-vars setup (`@radix-ui/react-slot`, `tailwindcss-animate`, canonical `globals.css`/`tailwind.config.ts`/`button.tsx`, `components.json` style `new-york`) for a more defensible, documented base. Use `shadcn@2` for future components. `.env.local.example` (NEXT_PUBLIC_API_URL/WS_URL/GOOGLE_CLIENT_ID), `frontend-ci.yml` (pnpm install --frozen-lockfile → lint → tsc --noEmit → build). Landing page renders; `pnpm build` + `lint` + `tsc --noEmit` all green. Note: pnpm 11 blocks build scripts by default — approved `unrs-resolver` via `allowBuilds` in `pnpm-workspace.yaml`.
- [x] Authored `FRONTEND_PLAN.md` — source-of-truth spec for the Next.js frontend, grounded in the *implemented* backend contract (not PROJECT_PLAN's aspirational one). Covers: stack, the real REST + WS event contract (incl. `?token=` WS auth + 4401 handling and the actual event types, which differ from PROJECT_PLAN §11), app/route/component structure, the Zustand-vs-TanStack state split, key flows, token handling, env, a 0→7 build order, testing, and deltas (map deferred, no `interrupt()` UI yet, PDF already server-side).
- [x] WebSocket auth + ownership: the chat WS now requires a JWT (`?token=` query param, since WS clients can't set Authorization headers) and is only accepted if the token is valid AND the caller owns the trip — otherwise closed with code 4401 before `accept()` (no bad-token/not-owner distinction, mirroring the REST 404 no-leak policy). `chat._authorize(trip_id, token)` opens a short-lived session (per-turn persistence still gets its own). Streaming tests bypass auth via a `_authorize` monkeypatch fixture; the gate itself has 5 focused tests (owner-ok, missing/empty token, bad token, non-UUID trip, non-owner) + a TestClient rejection test. This settles the WS connection contract before the frontend consumes it. 86 tests green, ruff + mypy strict clean.
- [x] Google OAuth (`POST /api/v1/auth/google`): verifies a Google Sign-In ID token (signature + audience against our `GOOGLE_CLIENT_ID`) via `app/core/google_oauth.py::verify_google_token` (google-auth; the crypto/network call isolated as a single mockable seam). Endpoint resolves the user by `google_id`, else links to an existing email account, else creates a Google-only user (no password). `UserRepository.get_by_google_id` + `create_google`. `GoogleAuthRequest` schema, `google_client_id` config. Added `google-auth` dep. Tests: 3 (create, link-existing-email, invalid-token 401) with the verifier mocked. Live-verify needs a real Google client id.
- [x] `currency` MCP server (FastMCP): `convert_currency(amount, from, to=INR)` over Frankfurter (`api.frankfurter.dev`, keyless ECB rates). Defensive parsing, same-currency short-circuit, `ConversionResult` contract. `app/mcp/currency.py` agent helper, registry entry (no secret). Tests: 4 adapter (rate math, identity, missing-rate, HTTP error) + 1 in-memory tool contract. LIVE verified end-to-end against the real API (100 USD → ₹9562).
- [x] `places` MCP server (FastMCP): `search_places(query, max_results)` over Google Places API (New) Text Search, minimal field mask (name/address/location/rating/id) to cap cost. Defensive parsing, `Place`/`PlacesResult` contract. `app/mcp/places.py` agent helper, registry entry passing `GOOGLE_MAPS_KEY` (least privilege). `google_maps_key` config. Tests: 4 adapter (parse, limit, missing-key, malformed) + 1 in-memory tool contract. Pool integration test now asserts all three tools discovered. Live-verify needs a Google Maps key.
- [x] Chat turn persistence + itinerary save: WS `_run_turn` now collects the turn's assistant text + streamed events and, after `complete`, `_persist_turn` writes the user + assistant `messages` rows (assistant carries the event log in `agent_events`) and saves the latest itinerary onto `trip.itinerary`. No-ops for non-UUID / unknown thread ids so the ad-hoc WS tests stay hermetic. `ConversationRepository.get_by_trip`. Tests: 2 (happy path writes messages+itinerary against test DB; unknown-thread no-op). Closes the loop between the live agent and the REST/DB layer (`GET /trips/{id}` and `/messages` now populate).
- [x] PDF export (`GET /trips/{id}/pdf`): pure `app/services/pdf.py::render_itinerary_pdf(Itinerary) -> bytes` over the SSOT schema (fpdf2, pure-Python, no native deps). Endpoint enforces ownership, 409 if no itinerary yet, returns `application/pdf` attachment. Defensive latin-1 encoding (fpdf core fonts) so ₹/emoji don't crash; renders as "Rs.". Tests: 4 (bytes/`%PDF` header, non-latin1 safety, 409 path, happy path). Added `fpdf2` dep.
- [x] Repo created, remote set to `Sujeethh03/TripPilot-AI`, project plan committed
- [x] Scaffold FastAPI backend: app factory, pydantic-settings config, `/api/v1/health`, CORS, Dockerfile, smoke test
- [x] LangGraph conversational graph skeleton: SSOT schemas, `ConversationState`, checkpointer factory, 7 stub nodes, routing + bounded refine loop, end-to-end tests. mypy strict + ruff clean.
- [x] `weather` MCP server (FastMCP) + MCP client pool: OpenWeatherMap adapter (daily aggregation, defensive parsing), `get_forecast` tool, stdio entrypoint, Dockerfile. Client pool via langchain-mcp-adapters with a server registry. Tests: unit (parsing), in-memory tool contract, integration (pool spawns server & discovers tool). All green.
- [x] Researcher wired to real MCP data: `app/mcp/weather.py::fetch_forecast` calls the weather tool through the pool and parses the result defensively; Researcher populates `ResearchBundle.weather_by_day` for the Synthesizer. All 7 nodes now real AND (for weather) grounded. LIVE verified: Munnar plan pulls real forecast into the bundle. 40 tests green, ruff + mypy strict clean.
- [x] Chat WebSocket + streaming (`/ws/trips/{id}/chat`): graph singleton (`get_graph`), streaming translation layer (`app/agents/streaming.py`: node updates → route_decision/agent_update/partial_itinerary/validation_report/assistant_message/complete/error), WS endpoint streaming via `graph.astream(stream_mode=updates)`, thread_id = trip id. Shared test fixture in `conftest.py`. Tests: streaming unit + WS integration (TestClient). LIVE verified: real streamed run showed the validator/refiner self-correction loop (day_count_mismatch → fixed). 47 tests green, ruff + mypy strict clean.
- [x] Trips REST API (`app/api/v1/trips.py`): `POST /trips` (creates trip + conversation, thread_id=trip.id), `GET /trips` (paginated), `GET /trips/{id}`, `PATCH /trips/{id}`, `DELETE /trips/{id}` (soft delete), `GET /trips/{id}/messages`. `TripRepository` + `ConversationRepository`/`MessageRepository`. Ownership enforced in the repo query (404, no existence leak). `TripCreate/Update/Response` + `MessageResponse` (from_attributes). `auth_headers`/`register_and_auth` test helpers. 6 integration tests (CRUD, ownership, auth-required). Fixed soft-delete tz bug (naive UTC to match columns). 61 tests green, ruff + mypy strict clean.
- [x] Email/password auth: `app/core/security.py` (bcrypt hashing + PyJWT HS256 tokens), `UserRepository` (Repository pattern), `get_current_user` dep (HTTP Bearer), endpoints `POST /api/v1/auth/register` (201, 409 on dup), `POST /api/v1/auth/login` (JWT), `GET /api/v1/me`. Auth schemas with `EmailStr`. Config: jwt_secret/algorithm/expiry. Test infra: Postgres-backed `client`/`db_sessionmaker` fixtures (fresh schema per test on `trippilot_test`), added Postgres service + `TEST_DATABASE_URL` to CI. Tests: security unit + 5 auth integration. 56 tests green, ruff + mypy strict clean. Google OAuth still TODO.
- [x] LangGraph Postgres checkpointer wired via FastAPI lifespan (`app/main.py`): builds the graph once per process with `AsyncPostgresSaver` when `CHECKPOINTER=postgres` + `DATABASE_URL` set, else `MemorySaver` (default, keeps tests hermetic). WS endpoint now reads `app.state.graph`; removed the `get_graph` singleton. Added `checkpointer` setting + `postgres_dsn` property. Added `langgraph-checkpoint-postgres` + `psycopg[binary]`. VERIFIED against local Postgres: ran a plan turn, then rebuilt saver+graph (simulated restart) and recovered full conversation state; checkpoint tables created by `setup()`. 47 tests green, ruff + mypy strict clean.
- [x] Persistence (DB layer): async SQLAlchemy 2.0 setup (`app/db/base.py` with naming convention, `app/db/session.py` engine/sessionmaker/`get_session` dep), models `users/trips/conversations/messages` (§10; UUID PKs, JSONB itinerary+agent_events, CHECK constraints, FK indexes, soft deletes), Alembic (async `env.py`, initial autogen migration). `docker-compose.yml` (pgvector Postgres + Redis + backend). Migrations exclude from ruff/mypy. VERIFIED against LOCAL Postgres (postgresql@17): migration applied, full async CRUD round-trip (user→trip→conversation→message with JSONB) read back. NOTE: Postgres started locally via `brew services start postgresql@17`; role/db `trippilot/trippilot`; `DATABASE_URL` added to `.env`. DB tests not yet in CI. 47 tests green, ruff + mypy strict clean.
- [x] Real Validator: deterministic rule chain in `app/agents/validation.py` (budget +10% buffer, pacing, time ordering, meal-slot presence, empty check) wired into the validator node. 11 exhaustive tests. Opening-hours/drive-time rules deferred (need places/directions MCP data).
- [x] GitHub Actions CI (`.github/workflows/backend-ci.yml`): ruff + mypy strict + pytest on push/PR touching `backend/**`.
- [x] Real Intake node: GPT-4o-mini structured extraction (`IntakeExtraction` → `TripRequest`). OpenAI client singleton (`app/core/openai_client.py`), model IDs in config, versioned prompt (`agents/prompts/intake.py`). LLM boundary isolated in `_extract` for mocking; graph converted to async (`ainvoke`). Added `openai` dep + `duration_days` to TripRequest. 23 tests green.
- [x] Real Planner + Synthesizer nodes (GPT-4o structured outputs). Shared `app/core/llm.py::parse_structured` helper (DRY across all 3 LLM nodes; single seam for future cost/retry/cache). Prompts `agents/prompts/planner.py`/`synthesizer.py`, `DaySkeletonPlan` wrapper schema. LLM boundaries (`_plan`, `_synthesize`) mocked in tests. Now the Intake→Planner→Synthesizer→Validator path is real (Researcher still a stub). 27 tests green.

## Log

### 2026-07-14
- Closed the backend "complete it" pass: WS auth (`?token=` + ownership, 4401 before accept), chat-turn persistence, PDF export (fpdf2), Google OAuth, and the `currency`/`places`/`directions` MCP servers.
- Grounded the Researcher: parallel weather + real Google Places searches (one per day theme) → `candidate_places`, which the Synthesizer prefers. Live-verified a Munnar plan now names real places (Pallivasal Tea Estate, Attukad/Nayamakad Waterfalls).
- Built the full frontend (Next.js 14, steps 0–7 in `FRONTEND_PLAN.md`): scaffold, contract layer, auth, dashboard, chat + WebSocket streaming, itinerary/budget, PDF. Live-verified the streaming turn end-to-end (self-correction loop visible; itinerary + messages persisted). Wrote root README run steps.

### 2026-07-13
- Set up project: git repo, `CLAUDE.md`, `PROGRESS.md`, copied in `PROJECT_PLAN.md`.
- Scaffolded backend under `backend/`: `pyproject.toml` (uv, fastapi, ruff/mypy/pytest), `app/main.py` (app factory + CORS + versioned router), `app/config.py` (pydantic-settings), `app/api/v1/health.py`, `.env.example`, `Dockerfile`, health smoke test, `backend/README.md`.
- Built LangGraph skeleton. Schemas (SSOT): `schemas/itinerary.py` (Itinerary/Day/Block), `schemas/trip.py` (TripRequest), `schemas/planning.py` (DaySkeleton, ResearchBundle, ValidationReport). Agents: `agents/state.py` (`ConversationState` TypedDict + `Route` StrEnum + `MAX_REFINEMENTS=2`), `agents/checkpointer.py` (MemorySaver now, Postgres later), `agents/nodes/*` (7 stub nodes, no LLM/MCP yet), `agents/graph.py` (router + linear pipeline + bounded validate/refine loop). Tests in `tests/test_graph.py`. Added `langgraph>=0.2` dep.
- Built `weather` MCP server + client pool. `mcp_servers/weather/` (schemas, `openweather.py` adapter, `server.py`, `__main__.py`, Dockerfile). `app/mcp/` (`config.py` registry, `client_pool.py` singleton over langchain-mcp-adapters). Tests under `tests/mcp/` + `tests/test_mcp_pool.py`. Added `fastmcp>=2.0`, `langchain-mcp-adapters>=0.1`, `httpx>=0.27` deps. `OPENWEATHER_KEY` in `.env.example`.
- Verified the weather path hits the real OpenWeatherMap API (got a genuine 401 — key not yet active/invalid; code path confirmed correct). Moved the key from tracked `.env.example` into gitignored `.env`.
- Implemented the real Validator (`app/agents/validation.py`) as an ordered rule chain + 11 tests. Added backend CI workflow. Full suite: 20 tests green, ruff + mypy strict clean.
- Implemented the real Intake node with OpenAI structured outputs. New: `app/core/openai_client.py` (singleton), `app/agents/prompts/intake.py`, `IntakeExtraction` schema, `model_router`/`model_planner` config. Graph now runs async. LLM mocked in tests (no key/network needed). Note: `duration_days` added to `TripRequest` (additive). Live verification pending an `OPENAI_API_KEY`. 23 tests green.
- Implemented Planner + Synthesizer. Factored shared parse boilerplate into `app/core/llm.py::parse_structured` and refactored Intake onto it. Prompts + `DaySkeletonPlan` wrapper. Real LLM path is now Intake→Planner→Synthesizer→Validator (Researcher stubbed). Tests mock each LLM boundary via `importlib` module patch (node functions shadow their submodules). 27 tests green, ruff + mypy strict clean.
- LIVE verified end-to-end against real OpenAI (key set): "3 days Kerala 20k couple waterfalls/street food" → correct Intake extraction, 3 sensible day themes from Planner, valid itinerary. Known gap: Synthesizer only filled Day 1 (no rule enforcing day count == duration_days yet).
- LIVE verified weather: OpenWeather key now active. Fixed MCP env passing — stdio transport sanitizes env, so `app/mcp/config.py` now passes `OPENWEATHER_KEY` (from settings) + safe base vars to the weather subprocess (least privilege). Real forecasts confirmed via direct client AND full pool→server→API path (Kochi, Munnar). Added `openweather_key` to Settings.
- Completed the graph: real Conversation Manager (GPT-4o-mini router → chit_chat/clarify/plan/refine, inline replies, resets per-turn refinement counter) and real Refiner (GPT-4o revision from validation issues + user message). Added `check_day_count` Validator rule + tightened Synthesizer prompt (fixes the Day-1-only bug). All 7 nodes now real. 35 tests green, ruff + mypy strict clean.
- LIVE verified the full graph: "3 days Kerala 20k couple waterfalls/street food" → complete 3-day itinerary (₹9300, valid). Refine turn "swap day 3 for a beach day" → correctly routed to refine, kept days 1-2, rebuilt day 3 as Marari Beach. Multi-turn conversational planning works end to end.
- Added WebSocket auth: `chat._authorize` gates the connection on a valid JWT (`?token=`) + trip ownership, closing with 4401 before accept otherwise. Settles the WS connection contract before the frontend. Then authored `FRONTEND_PLAN.md` (grounded in the real backend contract) and repointed PROGRESS to it. Backend MVP considered complete; next work is the frontend. 86 tests green, ruff + mypy strict clean.
- Completed the "complete backend" pass: (1) chat turn persistence — WS now writes user+assistant messages (event log in `agent_events`) and saves the itinerary onto `trip.itinerary` via `_persist_turn`, no-op for non-UUID threads so WS tests stay hermetic; (2) PDF export `GET /trips/{id}/pdf` via pure `app/services/pdf.py` (fpdf2, latin-1-safe, 409 if no itinerary); (3) Google OAuth `POST /auth/google` (google-auth ID-token verification, upsert/link user); (4) `currency` MCP server (Frankfurter, keyless, LIVE verified 100 USD→₹9562) and `places` MCP server (Google Places Text Search, contract-tested). Pool test asserts all 3 tools. Fixed fpdf2 multi_cell cursor bug (explicit new_x/new_y). 80 tests green, ruff + mypy strict clean. Remaining: directions MCP (needs Maps key), buses MCP (Rome2Rio gated), wiring places/currency into Researcher+Validator, frontend.
